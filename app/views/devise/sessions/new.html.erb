<h2>Sign in</h2>

<%= render 'layouts/errors', object: resource %>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>

  <div class="form-group">
    <div class="col-md-6">
      <%= f.text_field :username, autofocus: true, class: "form-control", placeholder: 'Username' %>
    </div>
  </div><br><br><br>

  <div class="form-group">
    <div class="col-md-6"><%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "Password" %></div>
  </div><br><br>

  <div class="submit"><%= f.submit "Sign up", class: 'btn btn-success' %></div>
<% end %>

<%= render "devise/shared/links" %>
